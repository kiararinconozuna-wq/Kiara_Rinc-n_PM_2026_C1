/* Estructura repetitiva while.
El conjunto de instrucciones muestra la sintaxis de la estructura while en el 
lenguaje c.*/
PI = proposicion incial;
while (PI) // PI debe tener un valor verdadero para que el siclo se ejecute.
{
    proceso; //cueerpo es la estructura de while
    PI = modificación de PI;
}